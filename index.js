

var commands = require('./build/commands');


// use underscore to get an array of {key:words}
// take an md5 of command.js file and add it as a key.

// when speech is entered loop through objects
// match words, run command, command returns action, perform action.
