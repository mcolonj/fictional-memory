{
  words:['run', 'sound'],
  command: function(text) {
    // get spoken text and filter for needed information.
    var commandText = undefined;
    var action = function(text) {
      // will be returned to processor for execution.

    };
    return action;
  }
},
