const assert = require('assert');
var finance = require('../tools/sentimentifying');

describe('Testing financial functions', function() {

  it('Testing Sentiment of sentence', function() {
      // no tests yet
      assert.equal(true, true);
  });
});
